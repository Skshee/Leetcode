arr.sort() sorts the list in place and returns None.

sorted() returns a new sorted list without modifying the original.

Sort in descending : arr.sort(reverse=True) -> same array now in descending order
                   : desc = sorted(arr, reverse=True) -> Stores new 